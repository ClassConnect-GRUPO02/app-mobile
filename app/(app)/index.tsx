import React from 'react';
import { View, Text } from 'react-native';
//import HomeScreen from './home';
import HomeScreen from "@/app/(courses)";

export default function AppIndex() {
    return <HomeScreen />;
}